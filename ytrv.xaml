<Window x:Class="WpfApp5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5"
        mc:Ignorable="d"
        Title="лень ставить пнг да и не смотрится" Height="450" Width="800" Icon="/6sNaa3IbnTr48447a-xxl.gif" ResizeMode="NoResize">
    <Window.Background>
        <ImageBrush/>
    </Window.Background>
    <Grid Margin="0,10,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="257*"/>
            <ColumnDefinition Width="543*"/>
        </Grid.ColumnDefinitions>
        <Menu Grid.ColumnSpan="2">
            <MenuItem Header="Дейвствия">
                <MenuItem Header="Рисовать" Click="draw"/>
                <MenuItem Header="Удалить" Click="del"/>
                <MenuItem Header="Цвет" Click="collor"/>
                <Separator></Separator>
                <MenuItem Header="Выход" Click="ext"/>
                <MenuItem Header="О программе" Click="prog"/>
            </MenuItem>
        </Menu>
        <Canvas x:Name="Canvas1" Margin="10,10,0,0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Height="400" VerticalAlignment="Top" Width="775">
        </Canvas>

    </Grid>
</Window>
